{
  "type": "module",
  "scripts": {
    "transpile": "jco transpile --optimize ../target/wasm32-wasip2/release/tree_sitter_query_formatter.wasm -o transpiled",
    "dev": "npm run transpile && vite",
    "build": "npm run transpile && vite build --base=/tree-sitter-query-formatter/",
    "clean": "rm -rf node_modules dist transpiled"
  },
  "devDependencies": {
    "@bytecodealliance/jco": "^1.15.0",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "monaco-editor": "^0.53.0",
    "lz-string": "^1.5.0"
  }
}
